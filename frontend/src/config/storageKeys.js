/** Keys persisted in localStorage — keep in sync across auth + navbar. */
export const LANCERAI_ACCESS_TOKEN = 'lancerai_access_token';
export const LANCERAI_USER_PROFILE = 'lancerai_user_profile';
/** Legacy mock key — cleared on logout to avoid stale sessions */
export const LANCERAI_MOCK_USER_LEGACY = 'mockUserDB';
