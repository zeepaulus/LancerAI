# ========================
# Application Settings
# ========================
APP_ENV=production
APP_DEBUG=false
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_TO_FILE=false
# LOG_DIR=/var/log/lancerai

# ========================
# Auth / JWT
# ========================
# ⚠️  THAY BẰNG KEY MẠNH HƠN: openssl rand -hex 32
AUTH_SECRET_KEY=REPLACE_WITH_STRONG_SECRET_MIN_32_CHARS
AUTH_JWT_ALGORITHM=HS256
AUTH_ACCESS_TOKEN_TTL_MINUTES=60
AUTH_REFRESH_TOKEN_TTL_DAYS=7
AUTH_ALLOW_WEAK_SECRET=false

# ========================
# CORS — thay YOUR_DROPLET_IP bằng IP thật
# ========================
ALLOWED_ORIGINS=["http://YOUR_DROPLET_IP","http://YOUR_DROPLET_IP:80"]
FRONTEND_BASE_URL=http://YOUR_DROPLET_IP

# ========================
# Database (PostgreSQL)
# ⚠️  Đổi hostname localhost → postgres (Docker service name)
# ⚠️  Đổi password postgres → password mạnh hơn
# ========================
DATABASE_URL=postgresql+asyncpg://postgres:REPLACE_PG_PASSWORD@postgres:5432/lancerai
DATABASE_ECHO=false
POSTGRES_DB=lancerai
POSTGRES_USER=postgres
POSTGRES_PASSWORD=REPLACE_PG_PASSWORD

# ========================
# Redis — đổi localhost → redis (Docker service name)
# ========================
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/1
CELERY_RESULT_BACKEND=redis://redis:6379/2

# ========================
# Vector Database (ChromaDB)
# ⚠️  Đổi localhost → chromadb, port 8001 → 8000 (internal Docker port)
# ========================
VECTOR_DB_HOST=http://chromadb
VECTOR_DB_PORT=8000
VECTOR_DB_COLLECTION=cv_embeddings
VECTOR_DB_BACKEND=chroma
VECTOR_DB_API_KEY=

# ========================
# Knowledge Graph (Neo4j)
# ⚠️  Đổi localhost → neo4j (Docker service name)
# ⚠️  Đổi password (giữ nguyên cũng được nhưng nên mạnh hơn)
# ========================
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=REPLACE_NEO4J_PASSWORD
NEO4J_AUTH=neo4j/REPLACE_NEO4J_PASSWORD

# ========================
# Local LLM (Ollama)
# ⚠️  Droplet CPU thường không đủ để chạy Ollama — để nguyên localhost
#    Nếu không cài Ollama trên droplet, app sẽ tự fallback sang NVIDIA/Groq
# ========================
LLM_LOCAL_BASE_URL=http://localhost:11434
LLM_LOCAL_MODEL=qwen2.5:3b

# ========================
# Self-hosted Cloud LLM (bỏ trống nếu không dùng)
# ========================
LLM_HOSTED_BASE_URL=
LLM_HOSTED_API_KEY=
LLM_HOSTED_MODEL=
LLM_HOSTED_EMBEDDING_MODEL=

# ========================
# Cloud LLM Fallback (Groq API)
# ⚠️  Điền API key thật từ console.groq.com (miễn phí)
# ========================
LLM_CLOUD_API_KEY=REPLACE_GROQ_API_KEY
LLM_CLOUD_BASE_URL=https://api.groq.com/openai/v1
LLM_CLOUD_MODEL=llama-3.1-70b-versatile

# ========================
# NVIDIA NIM API — giữ nguyên key từ .env local
# ========================
LLM_NVIDIA_API_KEY=REPLACE_NVIDIA_API_KEY
LLM_NVIDIA_BASE_URL=https://integrate.api.nvidia.com
LLM_NVIDIA_MODEL=meta/llama-3.1-8b-instruct
LLM_NVIDIA_MAX_TOKENS=16384
LLM_NVIDIA_ENABLE_THINKING=false

# ========================
# LLM Cache
# ========================
LLM_CACHE_ENABLED=true
LLM_CACHE_SIMILARITY_THRESHOLD=0.92

# ========================
# STT (Speech-to-Text) — Faster-Whisper trên CPU
# Dùng "tiny" thay vì "small" để tiết kiệm RAM trên droplet 4GB
# ========================
STT_MODEL_SIZE=tiny
STT_COMPUTE_TYPE=int8
STT_LANGUAGE=vi
STT_DEVICE=cpu

# ========================
# VAD
# ========================
VAD_SILENCE_THRESHOLD_MS=1300
VAD_MIN_SPEECH_DURATION_MS=200

# ========================
# TTS
# ⚠️  VieNeu cần file model .gguf (~1GB) trên server
#    Nếu chưa upload model → đổi sang edge-tts (không cần model file, dùng cloud Microsoft)
#    edge-tts: hoàn toàn miễn phí, không cần RAM local, chỉ cần internet
# ========================
TTS_ENGINE=edge
TTS_VOICE=vi-VN-HoaiMyNeural
# TTS_MODEL_PATH=./models/tts/vieneu-turbo/vieneu-tts-v2-turbo.gguf
